# This project primarily uses Python's built-in functionalities.
# No external pip-installable libraries are strictly required beyond the Python standard library.
# The 'math' module is part of the standard library.
# The 'datetime' module is also part of the standard library.
